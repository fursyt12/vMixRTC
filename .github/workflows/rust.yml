name: vMixRTC

# Сборка vMixRTC: Windows (MSVC → .msi/.exe), macOS (.dmg) и Linux (.deb/.AppImage).
# Плюс тесты и проверка переноса на всех трёх платформах.
#
# Запуск: вручную (workflow_dispatch) или тегом:
#   rust-v0.1.0        → GitHub Release с артефактами
#   rust-v0.1.0-alpha  → prerelease (alpha/beta/rc помечаются автоматически)
#   rust-test-*        → prerelease для проверки
on:
  workflow_dispatch:
    inputs:
      version:
        description: "Версия для имени артефакта (например 0.1.0)"
        required: false
        default: "0.1.0"
  push:
    tags:
      - "rust-v*"
      - "rust-test-*"

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Тесты (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Зависимости Linux (WebKitGTK, ALSA, HID)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libgtk-3-dev \
            libayatana-appindicator3-dev librsvg2-dev libasound2-dev libudev-dev

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Тесты
        run: cargo test --workspace

      - name: Проверка переноса примеров (чтение → запись → чтение)
        run: cargo run -q -p vmixrtc-cli -- verify examples

  bundle:
    name: Сборка приложения (${{ matrix.os }})
    needs: test
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Windows-инсталлятор (.msi через WiX + .exe)
          - os: windows-latest
            artifact: vMixRTC-windows
          - os: macos-latest
            artifact: vMixRTC-macos
          - os: ubuntu-latest
            artifact: vMixRTC-linux
    steps:
      - uses: actions/checkout@v4

      - name: Зависимости Linux
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libgtk-3-dev \
            libayatana-appindicator3-dev librsvg2-dev libasound2-dev libudev-dev

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Tauri CLI
        run: cargo install tauri-cli --version "^2" --locked

      - name: Сборка пакета
        working-directory: crates/vmixrtc
        run: cargo tauri build

      - name: Артефакты (Windows)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: |
            target/release/bundle/msi/*.msi
            target/release/bundle/nsis/*.exe
            target/release/vmixrtc.exe

      - name: Артефакты (macOS)
        if: runner.os == 'macOS'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: |
            target/release/bundle/dmg/*.dmg
            target/release/bundle/macos/*.app

      - name: Артефакты (Linux)
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: |
            target/release/bundle/deb/*.deb
            target/release/bundle/appimage/*.AppImage
            target/release/vmixrtc

      - name: CLI (vmixrtc-cli) для платформы
        uses: actions/upload-artifact@v4
        with:
          name: vmixrtc-cli-${{ matrix.os }}
          path: |
            target/release/vmixrtc-cli
            target/release/vmixrtc-cli.exe

  release:
    name: GitHub Release
    needs: bundle
    if: startsWith(github.ref, 'refs/tags/rust-v') || startsWith(github.ref, 'refs/tags/rust-test-')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Собрать zip-архивы
        run: |
          cd artifacts
          for dir in */; do
            name="${dir%/}"
            (cd "$dir" && zip -r "../${name}.zip" .)
          done

      - name: Опубликовать релиз
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*.zip
          prerelease: ${{ startsWith(github.ref, 'refs/tags/rust-test-') || contains(github.ref, '-alpha') || contains(github.ref, '-beta') || contains(github.ref, '-rc') }}
          generate_release_notes: true
          name: ${{ startsWith(github.ref, 'refs/tags/rust-test-') && 'vMixRTC test build' || 'vMixRTC' }}
